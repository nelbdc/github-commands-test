name: Call Reusable
on: push
jobs:
  call-reusable:
    runs-on: ubuntu-latest
    steps:
      - name: call reusable workflow
        uses: ./.github/workflows/reusable.yml
        with:
          name: "Nelson"
          secrets: "secret"
          executed: true
